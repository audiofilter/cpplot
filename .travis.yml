os:
  - osx
language: 
  - cpp
compiler:
  - clang
before_install:
  - brew update
install:
  - brew install boost
# Change this to your needs
before_script:
  - mkdir build
  - cd build
  - cmake ..
script: make
